diff --git i/include/aquamarine/backend/DRM.hpp w/include/aquamarine/backend/DRM.hpp
index b39f739..5f076f7 100644
--- i/include/aquamarine/backend/DRM.hpp
+++ w/include/aquamarine/backend/DRM.hpp
@@ -467,8 +467,9 @@ namespace Aquamarine {
             Hyprutils::Memory::CSharedPointer<CDRMRenderer> renderer; // may be null if creation fails
         } rendererState;

-        bool                                                          rendererRequired = true;
-        eBackendGPUDriver                                             driver           = AQ_BACKEND_GPU_DRIVER_UNKNOWN;
+        bool                                                          rendererRequired   = true;
+        bool                                                          rendererInitFailed = false; // set once CDRMRenderer::attempt() fails, e.g. no DRM render node (hyperv_drm); stops retrying every frame commit
+        eBackendGPUDriver                                             driver             = AQ_BACKEND_GPU_DRIVER_UNKNOWN;

         Hyprutils::Memory::CWeakPointer<CBackend>                     backend;

diff --git i/src/backend/drm/DRM.cpp w/src/backend/drm/DRM.cpp
index 888fef2..40fea84 100644
--- i/src/backend/drm/DRM.cpp
+++ w/src/backend/drm/DRM.cpp
@@ -652,6 +652,9 @@ bool Aquamarine::CDRMBackend::initMgpu() {
     if (!rendererRequired)
         return true;

+    if (rendererInitFailed)
+        return false;
+
     SP<CGBMAllocator> newAllocator;
     if (primary || backend->primaryAllocator->type() != AQ_ALLOCATOR_TYPE_GBM) {
         newAllocator            = CGBMAllocator::create(backend->reopenDRMNode(gpu->fd), backend);
@@ -670,6 +673,7 @@ bool Aquamarine::CDRMBackend::initMgpu() {

     if (!rendererState.renderer) {
         backend->log(AQ_LOG_ERROR, "drm: initMgpu: no renderer");
+        rendererInitFailed = true;
         return false;
     }
